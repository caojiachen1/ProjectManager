<ui:FluentWindow
    x:Class="ProjectManager.Views.Dialogs.EnvironmentVariablesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:models="clr-namespace:ProjectManager.Models"
    Title="{DynamicResource EnvVarDialog_Title}"
    Width="800"
    Height="600"
    MinWidth="600"
    MinHeight="400"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResize"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <ui:FluentWindow.Resources>
        <!-- DataGrid样式资源 -->
    </ui:FluentWindow.Resources>

    <Grid>
        <ui:TitleBar
            x:Name="TitleBar"
            Title="{DynamicResource EnvVarDialog_Title}"
            Grid.Row="0"
            CloseWindowByDoubleClickOnIcon="True"
            Padding="5,0,0,0"> <!-- 调整文字的左移 -->
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="BracesVariable24" Margin="10,-3,10,0"/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Margin="16,32,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Margin="0,10,0,16">
            <TextBlock Text="{Binding Project.Name, StringFormat='{}{0}'}" 
                       FontSize="18" 
                       FontWeight="SemiBold"/>
            <TextBlock Text="{DynamicResource EnvVarDialog_Description}" 
                       Opacity="0.7" 
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- 搜索框 -->
        <ui:TextBox Grid.Row="1" 
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    PlaceholderText="{DynamicResource EnvVarDialog_Search}"
                    Margin="0,0,0,16">
            <ui:TextBox.Icon>
                <ui:SymbolIcon Symbol="Search24"/>
            </ui:TextBox.Icon>
        </ui:TextBox>

            <!-- 环境变量列表 -->
            <Grid Grid.Row="2" Margin="0,0,0,16">
                <!-- DataGrid -->
                <ui:DataGrid ItemsSource="{Binding FilteredVariables}"
                             SelectedItem="{Binding SelectedVariable}"
                             AutoGenerateColumns="False"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False"
                             GridLinesVisibility="Horizontal"
                             VerticalAlignment="Top"
                             HeadersVisibility="Column"
                             SelectionMode="Single">
                        <ui:DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                <Setter Property="Padding" Value="8,4"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </ui:DataGrid.ColumnHeaderStyle>
                        <ui:DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="4"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource AccentFillColorSecondaryBrush}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ui:DataGrid.CellStyle>
                        <ui:DataGrid.Style>
                            <Style TargetType="ui:DataGrid">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource ControlStrokeColorSecondaryBrush}"/>
                                <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource ControlStrokeColorSecondaryBrush}"/>
                                <Setter Property="AlternatingRowBackground" Value="{DynamicResource SubtleFillColorSecondaryBrush}"/>
                                <Setter Property="RowBackground" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding EnvironmentVariables.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ui:DataGrid.Style>
                        <ui:DataGrid.Columns>
                            <DataGridTemplateColumn Header="{DynamicResource EnvVarDialog_Enabled}" Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type models:EnvironmentVariable}">
                                        <CheckBox IsChecked="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}" 
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTemplateColumn Header="{DynamicResource EnvVarDialog_Name}" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type models:EnvironmentVariable}">
                                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                                                 BorderThickness="0"
                                                 Background="Transparent"
                                                 VerticalAlignment="Center"
                                                 Padding="4"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTemplateColumn Header="{DynamicResource EnvVarDialog_Value}" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type models:EnvironmentVariable}">
                                        <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" 
                                                 BorderThickness="0"
                                                 Background="Transparent"
                                                 VerticalAlignment="Center"
                                                 Padding="4"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTemplateColumn Header="{DynamicResource EnvVarDialog_Actions}" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type models:EnvironmentVariable}">
                                        <ui:Button Content="{DynamicResource EnvVarDialog_Delete}"
                                                   Icon="{ui:SymbolIcon Delete24}"
                                                   Appearance="Danger"
                                                   Command="{Binding DataContext.RemoveVariableCommand, RelativeSource={RelativeSource AncestorType=ui:DataGrid}}"
                                                   CommandParameter="{Binding}"
                                                   Padding="8,4"
                                                   FontSize="12"
                                                   Height="28"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </ui:DataGrid.Columns>
                    </ui:DataGrid>
                    
                <!-- 空状态 -->
                <StackPanel HorizontalAlignment="Center" 
                            VerticalAlignment="Top"
                            Margin="32">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EnvironmentVariables.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <ui:SymbolIcon Symbol="Settings24" 
                                   FontSize="48" 
                                   Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                   Margin="0,0,0,16"/>
                    <TextBlock Text="{DynamicResource EnvVarDialog_NoVariables}" 
                               FontSize="16" 
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{DynamicResource EnvVarDialog_AddDescription}" 
                               Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

        <!-- 添加新变量 -->
        <Grid Grid.Row="3" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:TextBox Grid.Column="0" 
                        Text="{Binding NewVariableName, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="{DynamicResource EnvVarDialog_NamePlaceholder}"
                        Margin="0,0,8,0"/>
            
            <ui:TextBox Grid.Column="1" 
                        Text="{Binding NewVariableValue, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="{DynamicResource EnvVarDialog_ValuePlaceholder}"
                        Margin="0,0,8,0"/>
            
            <ui:Button Grid.Column="2" 
                       Content="{DynamicResource Button_Add}"
                       Icon="{ui:SymbolIcon Add24}"
                       Appearance="Primary"
                       Command="{Binding AddVariableCommand}"
                       Margin="0,0,8,0"/>
            
            <ui:Button Grid.Column="3" 
                       Content="{DynamicResource EnvVarDialog_ClearAll}"
                       Icon="{ui:SymbolIcon Delete24}"
                       Appearance="Danger"
                       Command="{Binding ClearAllVariablesCommand}"/>
        </Grid>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <ui:Button Content="{DynamicResource Button_Cancel}" 
                       Appearance="Secondary"
                       Click="CancelButton_Click"
                       Margin="0,0,8,0"
                       Padding="24,8"/>
            <ui:Button Content="{DynamicResource Button_Save}" 
                       Appearance="Primary"
                       Click="SaveButton_Click"
                       Padding="24,8"/>
        </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
